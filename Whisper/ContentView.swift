//
//  ContentView.swift
//  Whisper
//
//  Created by Anurag Ambuj on 28/12/25.
//

import SwiftUI

// This file is no longer used but kept empty to avoid build errors if referenced elsewhere by XCode's project file.
// Ideally, the file reference should be removed from the project.pbxproj, but we can't edit that safely.
// Leaving it as an empty View wrapper or just comments is safest.

struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
